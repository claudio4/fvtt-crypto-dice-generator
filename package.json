{
  "scripts": {
    "build": "esbuild --bundle --minify --sourcemap --format=esm --target=esnext --outdir=dist/scripts --out-extension:.js=.mjs scripts/main.mjs",
    "lint": "eslint scripts/*.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.17.19",
    "eslint": "^8.29.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.26.0"
  }
}
